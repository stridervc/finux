* Change keyboard driver to use unicode, not ascii
* gets() from keyboard driver
  * keyboard driver should log until enter pressed
  * look into creating syscalls (like linux)
* Allocate memory
* Show keyboard scan codes on screen
* Dump memory
* Better keyboard driver
* Read from filesystem (ls)
  * IDE
    * PCI
* Validate ACPI signature and checksum RSDT
* Output boot messages to serial
* Use serial output to write automated tests
